<h1>Listing map_comments</h1>

<table>
  <tr>
    <th>Map</th>
    <th>User</th>
    <th>Content</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @map_comments.each do |map_comment| %>
  <tr>
    <td><%= map_comment.map %></td>
    <td><%= map_comment.user %></td>
    <td><%= map_comment.content %></td>
    <td><%= link_to 'Show', map_comment %></td>
    <td><%= link_to 'Edit', edit_map_comment_path(map_comment) %></td>
    <td><%= link_to 'Destroy', map_comment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Map comment', new_map_comment_path %>
