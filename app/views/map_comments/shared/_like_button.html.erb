<% if current_user.likes?(comment) %>
    <%= link_to '| Sluta gilla ', map_map_comment_toggle_like_path(map, comment), :method => :post, :remote => true %>
<% else %>
    <%= link_to '| Gilla ', map_map_comment_toggle_like_path(map, comment), :method => :post, :remote => true %>
<% end %>