<% if current_user.likes?(update) %>
    <%= link_to '| Sluta gilla ', map_status_update_toggle_like_path(map, update), :method => :post, :remote => true %>
<% else %>
    <%= link_to '| Gilla ', map_status_update_toggle_like_path(map, update), :method => :post, :remote => true %>
<% end %>