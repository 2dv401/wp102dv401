<h2>Skicka upplåsningsinstruktioner</h2>

<%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
  <%= devise_error_messages! %>
	<p>Ett epostmeddelande med en upplåsningslänk och instruktioner på hur du skall gå till väga kommer att skickas till din epostadress.</p>
  <div class="email">
  	<%= f.label :email, "Epost" %>
  	<%= f.email_field :email %>
  </div>

  <div><%= f.submit "Skicka" %></div>
<% end %>

<%= link_to "Tillbaka", :back %>
