<% if current_user.likes?(comment) %>
    <%= link_to '| Sluta gilla ', map_status_update_status_comment_toggle_like_path(map, update, comment), :method => :post, :remote => true %>
<% else %>
    <%= link_to '| Gilla ', map_status_update_status_comment_toggle_like_path(map, update, comment), :method => :post, :remote => true %>
<% end %>