<% title 'Ändra i ' + @map.name %>
<h2>Redigera karta</h2>
<div id="mapCreation">
  <%= form_for(@map, url: map_path(@map.id), method: :put, html: { id: "map_form" }) do |f| %>
      <%= render 'maps/shared/form', f: f %>
  <% end %>
</div>